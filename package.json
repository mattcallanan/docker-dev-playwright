{
  "name": "docker-dev-playwright",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Playwright development container with top-level await support",
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "tsx": "^4.19.2"
  }
}
